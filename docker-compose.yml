version: '3.8'

services:
  council-recorder:
    build: .
    container_name: calgary-council-recorder
    volumes:
      - ./recordings:/app/recordings
      - ./data:/app/data
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - TZ=America/Edmonton
